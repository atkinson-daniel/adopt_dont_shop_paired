<h2>Update Information</h2>

<%= render partial: 'form', locals: { path: "/pets/#{@pet.id}",
                                      method: :patch,
                                      button_name: "Update Pet",
                                      pet: @pet} %>
